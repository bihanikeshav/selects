# Project TODO

- [ ] Cache Doctor quality buckets (underexposed, overexposed, out of focus, and blurry keepers) after the first computation. Key cached results by source fingerprint and processor version so changed files or scoring logic invalidate only the affected entries.
